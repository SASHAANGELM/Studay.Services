export enum ResponseStatus {
  SUCCESS = 'success',
  ERROR = 'error'
}

export enum UserRole {
  ADMIN = 'admin',
  TEACHER = 'teacher',
  STUDENT = 'student',
  PARENT = 'parent',
  PENDING = 'pending'
}

export enum OrganizationType {
  SCHOOL = 'school',
  OTHER = 'other'
}